# Composable Me Hydra - Python Dependencies

# Core orchestration
# Floor raised to 1.15.5: versions <1.15.5 are affected by CVE-2026-2275
# (Code Interpreter RCE), CVE-2026-2285 (arbitrary local file read via the
# JSON loader), and CVE-2026-2286 (SSRF via RAG tools).
crewai[anthropic]>=1.15.5
crewai-tools>=1.15.5

# LLM providers
openai>=1.0.0  # Transitive/runtime dependency of crewai for API compatibility
# Floor raised to 1.83.0: v1.82.7/1.82.8 were a compromised PyPI supply-chain
# release (credential-stealing malware, pulled ~40 min after publish); 1.83.0
# is the first clean release after that incident.
litellm>=1.83.0  # Required for OpenRouter and other non-native providers

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
rich>=13.0.0  # Better console output

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
hypothesis>=6.0.0

# OpenTelemetry - Observability
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation>=0.41b0
